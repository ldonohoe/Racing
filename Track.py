class Track(object):
	def __init_(self, screen, preset=0):
		# Need set two sets of points, 
		self.innerPoints
		self.outerPoints
		self.screenWidth = screen.get_width()
		self.screenheight = screen.get_height()